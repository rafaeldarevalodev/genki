# environment.yml
# Crear entornos con: conda env create -f environment.yml
---
name: genki
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
    - genkit
    - dotenv
    - numpy>=1.24.0

---
name: voxtral_audio
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - pip:
    - mlx-audio
    - "mistral-common[audio]"
    - numpy>=1.24.0