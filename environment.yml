# environment.yml
# Crear entornos con: conda env create -f environment.yml
---
name: genki
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
    - genkit
    - dotenv
    - numpy>=1.24.0
    - fastapi>=0.115.0
    - uvicorn[standard]>=0.30.0
    - python-multipart>=0.0.9
    - f5-tts-mlx>=0.2.6
    - mlx-whisper
    - requests>=2.31.0

---
name: voice_eval
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
    - fastapi>=0.115.0
    - uvicorn>=0.30.0
    - python-multipart>=0.0.9
    - soundfile>=0.12.0
    - sounddevice>=0.5.0
    - httpx>=0.27.0
    - pydantic>=2.0
    - numpy>=1.24.0
    - mlx-whisper

---
name: vibevoice7b
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
    - mlx
    - huggingface_hub[hf_xet]
    - numpy>=1.24.0
    - soundfile>=0.12.0